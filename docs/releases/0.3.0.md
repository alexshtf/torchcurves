**Breaking changes**

- replaced `normalize_fn` and `normalization_scale` with `input_map`
- added `torchcurves.maps` with dotted presets such as `real.rational`, `nonneg.rational`, and `nonneg.arctan`
- added `parameter_range` for generated uniform B-spline knots
- updated README, docs, and example notebooks to the new input-map API
